// Vitest global test setup: registers jest-dom's matchers (toBeInTheDocument, etc.)
// on Vitest's `expect`. Referenced via vite.config.ts's `test.setupFiles`.
import "@testing-library/jest-dom/vitest";
